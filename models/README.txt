Place your CAD exports here:
  spine.stl      (required — export from Fusion 360 in mm)
  platform.stl   (optional)

If spine.stl is missing, the simulator falls back to a cuboid placeholder
so you can test the pipeline before exporting.
